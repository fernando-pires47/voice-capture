services:
  api:
    restart: unless-stopped
    env_file:
      - .env
    environment:
      APP_ENV: prod
    ports:
      - "127.0.0.1:8000:8000"

  web:
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:80"
